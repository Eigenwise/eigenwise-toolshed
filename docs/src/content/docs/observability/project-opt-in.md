---
title: Per-project opt-in
description: Enable local telemetry only for the projects where it helps.
---

Observability is off until a project opts in. That keeps the choice close to the codebase and lets each project decide whether local usage data belongs in its development loop.

The full guide will show the opt-in command, the local files involved, and how to switch collection back off.
