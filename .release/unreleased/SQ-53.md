---
ref: SQ-53
title: Keep sibling executors launched from one session bound to their own tickets and worktrees
bump: patch
plugins: [sidequest]
---

Closes GitHub #298. A claim's dispatch token now settles a SubagentStart guess that attached a runtime to a same-executor sibling's reservation, so a sibling closing no longer ends a live executor's run and each write is judged against its own lease.
