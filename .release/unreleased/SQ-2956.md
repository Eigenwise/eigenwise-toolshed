---
ref: SQ-2956
title: Remove the inert worktree quarantine per-agent cap from board_config
bump: patch
plugins:
  - sidequest
---

`board_config` and its CLI/dispatch surfaces no longer accept a per-agent worktree quarantine retention cap; retention is age-only.
