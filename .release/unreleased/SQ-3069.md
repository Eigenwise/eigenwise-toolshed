---
ref: SQ-3069
title: Story log rotation happens on append
bump: patch
plugins:
  - sidequest
---

`story_log` now archives older live entries before an append would exceed the
briefing window. `full: true` returns archived history before the live log.
