# Workbench

- Generalized the `code-intel` MCP tools from `typescript_definition`, `typescript_references`, and `typescript_diagnostics` to `definition`, `references`, and `diagnostics`. Each call now selects its language server from the requested file extension.
