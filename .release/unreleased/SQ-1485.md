---
ref: SQ-1485
title: Isolate the unscoped-dispatch test from the local model gateway
bump: patch
plugins:
  - sidequest
---

The planning-depth warning test dispatched through whatever model gateway the machine happened to be running, so it passed locally and failed on CI where no gateway exists. It now reads a fixture catalog like every other dispatch test.
