---
ref: SQ-2465
title: Stop the MCP exit test racing its own stdin writer
bump: patch
plugins:
  - sidequest
---

The MCP server exit tests ignore EPIPE on the child's stdin, so a write that lands after the server's deliberate exit no longer fails the suite.
